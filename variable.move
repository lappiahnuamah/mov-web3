let num: u64 = 10;


let mut num2: u64 = 20;
let num2 = 30; // This will not work, as num2 is already mutable

integer u
string
bool

let address: address = "0x1234567890abcdef";

let private: bool = true;

If you want to use a variable in a different scope, you can use the `let` keyword to create a new variable with the same name. This will shadow the previous variable, allowing you to use a string

use std::string::{Self, String}
use std::string::String; - vector<u8>

//Arithmetic operations
let sum: u64 = 5 + 10;
let diff: u

use std::debug;

debug::print(x: &result1);

let age: u32 = 25;
let is_adult: bool = if age >= 18 {
    true
} else {
    false
};


use std::debug;

public fun add_and_trace(a: u64, b: u64): u64{
    let result: u64 : u64 = a + b;

    debug::print(result);
}


init functions are private function 
and just one. 
